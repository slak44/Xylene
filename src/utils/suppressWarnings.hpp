#ifndef SUPPRESS_WARNINGS_HPP
#define SUPPRESS_WARNINGS_HPP

// The only reason this file exists is because GCC manages to somehow give warnings from system headers

#include <algorithm>
#include <numeric>

#endif
